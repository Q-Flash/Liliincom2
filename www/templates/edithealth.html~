<ion-view view-title="Update Injuries" >
        <ion-content class="padding-top">
          <div class="list text-center padding">
          <!--  <i class="icon ion-ios-game-controller-b larger positive"></i> -->
          </div>
          <h1> Look out for Potatoes! </h1>
          <form novalidate="" name="playerForm" form-manager>
          <div class="list">
            <label class="item item-input item-floating-label"
            ng-class="{ 'has-error' : playerForm.txtplayerfname.$invalid && playerForm.$submitted }">
              <span class="input-label">Player First Name</span>
              <input name="txtplayerfname" type="text" ng-model="player.player_fname"
              placeholder="First Name"
              required>
            </label>
            <div class="form-errors" ng-messages="playerForm.txtplayerfname.$error" ng-if="playerForm.$submitted">
              <div class="form-error" ng-message="required"><p style="color:red">This field is required.</p></div>
            </div>


            <label class="item item-input item-floating-label"
              ng-class="{ 'has-error' : playerForm.txtplayerfname.$invalid && playerForm.$submitted }">
              <span class="input-label">Player Last Name</span>
              <input name="txtplayerlname" type="text" ng-model="player.player_lname"
              placeholder="Last Name"
              required>
            </label>
            <div class="form-errors" ng-messages="playerForm.txtplayerlname.$error" ng-if="playerForm.$submitted">
              <div class="form-error" ng-message="required"><p style="color:red">This field is required.</p></div>
            </div>

          <label class="item item-input item-floating-label"
          ng-class="{ 'has-error' : playerForm.txtplayerinjury.$invalid && playerForm.$submitted }">
            <span class="input-label">Injury</span>
            <input name="txtplayerinjury" type="text" ng-model="player.playerinjury"
            placeholder="Injury"
            required>
          </label>
          <div class="form-errors" ng-messages="playerForm.txtplayerinjury.$error" ng-if="playerForm.$submitted">
            <div class="form-error" ng-message="required"><p style="color:red">This field is required.</p></div>
          </div>


          <label class="item item-input item-floating-label"
          ng-class="{ 'has-error' : playerForm.txtrecoverydays.$invalid && playerForm.$submitted }">
            <span class="input-label">Recovery Days</span>
            <input name="txtrecoverydays" type="text" ng-model="player.recoverydays"
            placeholder="Recovery Days"
            required>
          </label>
          <div class="form-errors" ng-messages="playerForm.txtrecoverydays.$error" ng-if="playerForm.$submitted">
            <div class="form-error" ng-message="required"><p style="color:red">This field is required.</p></div>
          </div>


          </div>
          <div class="padding-top">
            <button ng-click="submit()"  class = "button button-block button-calm">
              Submit
            </button>
          </div>
        </form>

        </ion-content>
</ion-view>
